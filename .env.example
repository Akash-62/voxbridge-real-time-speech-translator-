# VoxBridge Environment Variables

# Local Development: Use local TTS server (run: node server-local.js)
VITE_GTTS_SERVER_URL=http://localhost:3002

# Production: Vercel will use /api/tts automatically (don't set this in production)
