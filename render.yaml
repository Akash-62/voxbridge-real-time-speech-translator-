services:
  - type: web
    name: voxbridge-gtts-server
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn gtts_server:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
